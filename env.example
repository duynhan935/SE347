# =========================
# CLIENT-SIDE (exposed to browser)
# =========================
# Any variable prefixed with NEXT_PUBLIC_ will be bundled into the frontend.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Public API base URL (browser → goes through Caddy/reverse-proxy)
# Format: https://host:port/api
NEXT_PUBLIC_API_URL=https://localhost:8443/api

# Public backend origin (no /api) used for OAuth redirect targets, etc.
NEXT_PUBLIC_BACKEND_ORIGIN=https://localhost:8443

# Realtime endpoints (browser)
NEXT_PUBLIC_WS_BASE_URL=https://localhost:8443
NEXT_PUBLIC_ORDER_SOCKET_URL=https://localhost:8443
NEXT_PUBLIC_ORDER_WS_BASE_URL=https://localhost:8443

# Optional aliases (supported by the code as fallbacks)
NEXT_PUBLIC_API_BASE_URL=https://localhost:8443/api
NEXT_PUBLIC_API_ORIGIN=https://localhost:8443


# =========================
# SERVER-SIDE ONLY (NOT exposed to browser)
# =========================
# Used by Next.js SSR when running inside Docker network.
# Format: http://service-name:port/api
API_INTERNAL_URL=http://api-gateway:8080/api


